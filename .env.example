# Database Configuration
#
# For Vercel Deployment (Recommended):
# - Get DATABASE_URL from Vercel Dashboard > Storage > Postgres > Connection String
# - Example: postgres://user:password@host:5432/database?sslmode=require
#
# For Prisma Data Platform (Connection Pooling):
# - Example: prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY
#
# For Local Development (Docker):
# - Use: postgresql://dev_user:dev_password@localhost:5432/masar_dev
# - Run: docker-compose up -d
#
DATABASE_URL="postgresql://user:password@localhost:5432/masar_dev"

# NextAuth
NEXTAUTH_SECRET="your-secret-key-generate-with-openssl-rand-hex-32"
NEXTAUTH_URL="http://localhost:3000"

# Auth
JWT_SECRET="your-jwt-secret-generate-with-openssl-rand-hex-32"

# Multi-tenancy
TENANT_IDENTIFIER="subdomain"

# Redis (for sessions and caching - optional for phase 1)
REDIS_URL="redis://localhost:6379"

# Email (for notifications - phase 8)
RESEND_API_KEY="your-resend-api-key"

# S3 (for file storage - optional)
S3_BUCKET="masar-files"
S3_REGION="us-east-1"
S3_ACCESS_KEY_ID="your-access-key"
S3_SECRET_ACCESS_KEY="your-secret-key"

# Environment
NODE_ENV="development"
